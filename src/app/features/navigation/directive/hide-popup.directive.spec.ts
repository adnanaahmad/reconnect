import { HidePopupDirective } from './hide-popup.directive';

describe('HidePopupDirective', () => {
  it('should create an instance', () => {
    const directive = new HidePopupDirective();
    expect(directive).toBeTruthy();
  });
});
