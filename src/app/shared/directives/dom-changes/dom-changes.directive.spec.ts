import { DomChangesDirective } from './dom-changes.directive';

describe('DomChangesDirective', () => {
  it('should create an instance', () => {
    const directive = new DomChangesDirective();
    expect(directive).toBeTruthy();
  });
});
